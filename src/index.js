import React from "react";
import ReactDOM from "react-dom/client";
import EstructurasDeControl from "./EstructurasDeControl";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <EstructurasDeControl />
  </React.StrictMode>
);
